[package]
name = "engine_check"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
patcher = { path = "../patcher" }
sha2 = { version = "0.11", default-features = false }
